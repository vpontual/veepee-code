Add a `minLength` validator to this validation library.

It takes the minimum length as an argument — `['minLength', 3]` in a rule set —
and reports `<field> must be at least <n> characters` when the value is too
short.

Follow the conventions the existing validators already use. Run the test suite
when you are done.
