Add a `debug` level to this logger.

`debug` is less severe than `info`, its prefix tag is `[DEBUG]`, and its ANSI
colour code is 90.

Follow the conventions already used by the existing levels. Run the test suite
when you are done.
