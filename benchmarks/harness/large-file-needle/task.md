`surchargeGb` in src/surcharge.js returns the wrong number — it is not using
the GB rate.

Every other surcharge function is correct. Do not change them, and do not
change src/rates.js. Run the test suite when you are done.
