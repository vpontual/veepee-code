Add support for the `PATCH` method to the router. It should return
`update <path>`, the same shape the other methods use.

Match the indentation style already used in the file you are editing. Run the
test suite when you are done.
