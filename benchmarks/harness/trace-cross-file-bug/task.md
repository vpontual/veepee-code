The `reportTotals` function returns wrong numbers for some orders, and the test
suite is failing. Run it, work out the real cause, and fix it.

Do not change the tests — they are correct and describe the intended behaviour.
