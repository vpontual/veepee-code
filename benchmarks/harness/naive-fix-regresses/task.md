The test suite in this project is failing. Run it, find out why, and fix the
implementation so the whole suite passes.

Do not change the tests — they are correct and describe the intended behaviour.
