{
  "tags": ["typecheck", "self-verify", "single-file"],
  "timeout_ms": 420000,
  "description": "The only signal is tsc, not vitest — the visible suite is green while the project does not compile. Measures whether the agent reaches for a verification channel the prompt names but the test script does not run, and whether it fixes the types instead of silencing them."
}
